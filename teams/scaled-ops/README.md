# scaled-ops skills

Skills scoped to the scaled-ops team. They sync only to teammates whose
`~/.claude/team` is `scaled-ops`. Drop a `SKILL.md` directory under `skills/`.
See `../README.md` for the full model.
