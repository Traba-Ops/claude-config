# worker-ops skills

Skills scoped to the worker-ops team. They sync only to teammates whose
`~/.claude/team` is `worker-ops`. Drop a `SKILL.md` directory under `skills/`.
See `../README.md` for the full model.
