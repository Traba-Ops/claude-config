# customer-ops skills

Skills scoped to the customer-ops team. They sync only to teammates whose
`~/.claude/team` is `customer-ops`. Drop a `SKILL.md` directory under `skills/`.
See `../README.md` for the full model, and `skills/customer-output-format/`
for a starter you can copy.
